function spook(option) {
  //random paskaa tähän
  //activeButton.classList.remove('sounds');
  //activeButton.classList.add('soundschosen');
}

'''
window.setInterval(function(){
  var rand = Math.random();
  var rand2 = Math.floor(Math.random() * 6);
  if (rand < 0.01) {
    spook(rand2);
  }
}, 5000);
